<h2>Create a list</h2>
<%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.submit "Add a list", class: "btn btn-primary" %>
<% end %>
<br>
<%= link_to "Back to movie lists", lists_path(@list) %>
