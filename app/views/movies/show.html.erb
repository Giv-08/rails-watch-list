<h1><%= @movie.title %></h1>
<%= link_to 'Edit', edit_movie_path(@movie), class: "btn btn-success" %>

 <%# <h2><strong><%= @movie.title %> • <%# @movie.rating %></strong></h2>
 <p><%# @movie.overview %></p>
 <h1>Add this movie in the list</h1>
<%# iterate restaurant and review in the form %>
<%= simple_form_for [@list, @movie] do |f| %>
  <%= f.input :title %>
  <%= f.input :rating %>
  <%= f.input :overview %>
  <%= f.submit "Submit movie to a list", class: "btn btn-primary" %>
<% end %>
<%= link_to "Back to lists", lists_path %>
<br>
<%= link_to "Back to movies", movies_path(@movie) %>
